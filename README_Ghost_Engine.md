# Stealth Zig Network Engine

Successfully completed Layer 3 refactor and RST suppression tests.

To verify the TCP handshake:
```bash
sudo ./verify.sh
```

Ensure you execute this inside the `doggystyle` directory.
